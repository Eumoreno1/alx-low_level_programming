#include <stdio.h>

/**
 * main - program starts here
 * Description: a program that prints the alphabet in lowercase
 * Return: Always 0
 */

int main(void)
{
	char a, z;
	alphabet = 'a';

	while (alphabet <= 'z')
	{
		putchar("%c ", alphabet);
		alphabet++;
	}
	return (0);
}
